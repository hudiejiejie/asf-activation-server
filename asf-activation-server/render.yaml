services:
  - type: web
    name: asf-activation-server
    env: node
    plan: free
    buildCommand: npm ci --only=production
    startCommand: node server.js
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
      - key: ADMIN_API_KEY
        generateValue: true
      - key: DATA_ROOT
        value: /opt/render/project/src/data
      - key: DB_FILE
        value: /opt/render/project/src/data/activation.db
      - key: BACKUP_ROOT
        value: /opt/render/project/src/data/backups
    disk:
      name: data
      mountPath: /opt/render/project/src/data
      sizeGB: 1